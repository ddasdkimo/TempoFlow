import 'package:flutter_test/flutter_test.dart';

void main() {
  testWidgets('App builds without error', (WidgetTester tester) async {
    // Placeholder test - will be expanded with proper tests
    expect(1 + 1, equals(2));
  });
}
